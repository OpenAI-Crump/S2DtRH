[project]
name = "rabbit-hole"
version = "0.2.0"
description = "Scan 2 Down the Rabbit Hole - A recursive directory structure scanner"
requires-python = ">=3.10"

[project.scripts]
rabbit-hole = "rabbit_hole.__main__:main"
